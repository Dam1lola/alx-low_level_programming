#!/bin/bash
#include <stdio.h>

/**
 * Description: main - prints out strings to stdout
 *  
 *  Return: Always 0 (Success)
 */
int main(void)
{
puts("\"programming is like building a multilingual puzzle");
return (0);
}
